FROM python:3.11-slim-bullseye

WORKDIR /app

COPY app.py /app

RUN apt-get -y update && pip install --upgrade pip && pip install -r requirements.txt
RUN python3 main.py > output.log 2>&1 &

EXPOSE 8501

ENTRYPOINT ["streamlit", "run", "app.py"]